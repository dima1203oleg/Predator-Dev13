apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.global.secretsRef.name }}
  labels:
    {{- include "predator-umbrella.labels" . | nindent 4 }}
type: Opaque
data:
  # Placeholder for global secrets.
  # In a real-world scenario, these would be managed by a secret management system
  # like Vault or ExternalSecrets, or injected via CI/CD.
  # Example:
  # API_KEY: {{ .Values.global.secrets.apiKey | b64enc | quote }}
  # DATABASE_PASSWORD: {{ .Values.global.secrets.databasePassword | b64enc | quote }}
