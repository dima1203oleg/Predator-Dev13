apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "qdrant.fullname" . }}-network-policy
  labels:
    app: {{ include "qdrant.name" . }}
spec:
  podSelector:
    matchLabels:
      app: {{ include "qdrant.name" . }}
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: predator-dev
        - podSelector:
            matchLabels:
              app: api
      ports:
        - protocol: TCP
          port: 6333
