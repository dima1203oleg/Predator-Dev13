apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "redis.fullname" . }}-network-policy
  labels:
    app: {{ include "redis.name" . }}
spec:
  podSelector:
    matchLabels:
      app: {{ include "redis.name" . }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: predator-dev
        - podSelector:
            matchLabels:
              app: workers
      ports:
        - protocol: TCP
          port: 6379
  egress:
    - to:
        - podSelector:
            matchLabels:
              app: postgres
      ports:
        - protocol: TCP
          port: 5432
