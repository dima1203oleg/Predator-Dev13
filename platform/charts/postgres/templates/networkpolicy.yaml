apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "postgres.fullname" . }}-network-policy
  labels:
    app: {{ include "postgres.name" . }}
spec:
  podSelector:
    matchLabels:
      app: {{ include "postgres.name" . }}
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: predator-dev
        - podSelector:
            matchLabels:
              app: api
      ports:
        - protocol: TCP
          port: 5432
