apiVersion: v1
kind: Service
metadata:
  name: {{ include "predator-app.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector: { app.kubernetes.io/name: {{ include "predator-app.name" . }} }
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8000
